[project]
name = "eukahub-core"
version = "0.1.0"
description = "Shared domain model: the metric config (single source of truth) and typed rollups."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/eukahub_core"]
